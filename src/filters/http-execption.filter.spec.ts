import { HttpExecptionFilter } from './http-execption.filter';

describe('HttpExecptionFilter', () => {
  it('should be defined', () => {
    expect(new HttpExecptionFilter()).toBeDefined();
  });
});
